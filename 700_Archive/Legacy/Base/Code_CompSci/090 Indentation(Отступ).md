# Блоки кода и отступы

Одной из наиболее отличительных особенностей Python является использование отступов для обозначения блоков кода. Рассмотрим оператор if из нашей простой программы проверки паролей:

```python
if pwd == 'apple':
    print('Logging on ...')
else:
    print('Incorrect password.')

print('All done!')
```

Строки print(‘Вход в систему ...’) и print(‘Неверный пароль.’) представляют собой два отдельных блока кода. Эти примеры имеют длину всего в одну строку, но Python позволяет писать блоки кода, состоящие из любого количества операторов.

Чтобы указать блок кода на Python, вы должны сделать отступ в каждой строке блока на одинаковую величину. Два блока кода в нашем примере if-statement имеют отступ в четыре пробела, что является типичным отступом для Python.

В большинстве других языков программирования отступы используются только для того, чтобы сделать код красивым. Но в Python он требуется для указания, к какому блоку кода принадлежит оператор. Например, окончательная печать (‘Все готово!’) не имеет отступа и поэтому не является частью блока else.

Программисты, знакомые с другими языками, часто ощетиниваются при мысли, что отступ имеет значение: многим программистам нравится свобода форматировать свой код так, как им заблагорассудится. Однако правила отступов в Python довольно просты, и большинство программистов уже используют отступы, чтобы сделать свой код читаемым. Python просто развивает эту идею на один шаг дальше и придает смысл отступам.

/
	tag: #dev 	
	link: [[MOC OF PYTHON]]
	source: [Отступ в Python с примерами (turbopages.org)](https://translated.turbopages.org/proxy_u/en-ru.ru.ff04626b-63677990-da81bf49-74722d776562/https/www.freecodecamp.org/news/indentation-in-python/)
	