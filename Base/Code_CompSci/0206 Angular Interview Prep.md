# Архитектура Angular 
Angular - фреймворк, не библиотека. Он задаёт правила игры: структура, DI, сборка, маршрутизация и т.д.
Главные строительные блоки:

| Элемент       | Что делает                                                                    | Пример                                       |
| ------------- | ----------------------------------------------------------------------------- | -------------------------------------------- |
| **Module**    | Группирует компоненты, сервисы и пайпы. Делит приложение на логические части. | `AppModule`, `AuthModule`, `DashboardModule` |
| **Component** | Отвечает за отображение и логику UI.                                          | `app.component.ts`                           |
| **Service**   | Содержит бизнес-логику и данные, чтобы не захламлять компонент.               | `UserService`, `ApiService`                  |
| **Directive** | Меняет поведение элемента.                                                    | `*ngIf`, `*ngFor`, `appHighlight`            |
| **Pipe**      | Преобразует данные прямо в шаблоне.                                           | `{{ date                                     |

Главная идея:

> Компоненты — это “внешность”, сервисы — “мозг”, модули — “организация тела”.

## Component: сердце Angular
Каждый компонент — это **три файла**:

- `.ts` — логика
    
- `.html` — шаблон
    
- `.css/.scss` — стили
    

``` ts
@Component({
  selector: 'app-hero',
  templateUrl: './hero.component.html',
  styleUrls: ['./hero.component.scss']
})
export class HeroComponent {
  name = 'Angular';
}

```

HTML:

`<h1>Hello {{ name }}!</h1>`

Angular связывает шаблон и логику через **Data Binding**:

|Тип|Синтаксис|Что делает|
|---|---|---|
|Interpolation|`{{ value }}`|Выводит данные|
|Property binding|`[property]="value"`|Передаёт данные в DOM|
|Event binding|`(event)="handler()"`|Реагирует на события|
|Two-way binding|`[(ngModel)]="value"`|Синхронизирует данные|
### Dependency Injection (DI)
**DI** — это как “Яндекс Еда для зависимостей”: ты не готовишь сервис сам, Angular доставляет его тебе.

Ты просто говоришь:

``` ts
constructor(private userService: UserService) {}

```

и Angular сам подсовывает нужный экземпляр `UserService`.

💡 Зачем это нужно:

- Код легче тестировать
    
- Меньше дублирования
    
- Можно переиспользовать зависимости
#### Lifecycle Hooks

Каждый компонент живёт **в цикле жизни**.  
Ты можешь “вмешаться” на любом этапе.

|Хук|Когда срабатывает|Пример|
|---|---|---|
|`ngOnInit()`|После инициализации|Подгружаем данные с API|
|`ngOnChanges()`|Когда изменяются входные данные (`@Input`)|Реагируем на изменения|
|`ngOnDestroy()`|Перед уничтожением компонента|Очищаем подписки|
|`ngAfterViewInit()`|После рендера шаблона|Работаем с DOM напрямую|

💡 Пример:
``` ts
ngOnInit() {
  this.userService.getUsers().subscribe(users => this.users = users);
}

ngOnDestroy() {
  this.subscription.unsubscribe();
}

```

##### RxJS — кровь Angular
Angular = асинхронность → RxJS = язык асинхронности.  
`Observable` — поток данных, на который ты подписываешься.
``` ts
this.http.get<User[]>('api/users')
  .subscribe(users => this.users = users);

```
Основные операторы:

- **map** — преобразовать поток
    
- **filter** — отфильтровать
    
- **switchMap** — переключить поток
    
- **catchError** — поймать ошибку
Пример:
``` ts
this.userService.getUser()
  .pipe(
    filter(user => !!user),
    map(user => user.name.toUpperCase())
  )
  .subscribe(console.log);

```
###### Общая картина: как всё работает вместе
1. **AppModule** поднимает приложение.
    
2. **Component** отображает данные.
    
3. **Service** достаёт их (например, через HTTP).
    
4. **Pipe/Directive** делают UI живым.
    
5. **RxJS** двигает потоки данных.
    

Это и есть “жизнь Angular-приложения”: реактивное, модульное, чистое.

###### Быстрые заметки 
1. `@NgModule` — это декоратор, который объединяет функциональность в логические блоки (модули).  
	Он говорит Angular: “вот компоненты, которые принадлежат этому модулю, и вот зависимости, которые им нужны”.

2. Двухстороннее связывание данных (`[(ngModel)]`)
	**Как это работает:**
	
	- Когда значение меняется в TS → Angular обновляет DOM (html).
	    
	- Когда пользователь меняет значение в DOM (input) → Angular обновляет свойство в TS.
	    
	
	То есть — **две стрелки, туда и обратно**.  
	Для этого Angular использует **FormsModule**. 

3. Разница между `Subject` и `BehaviorSubject`
	 **Subject** — просто поток, который можно подписать и излучать значения.  
	 **BehaviorSubject** — то же самое, но **хранит последнее значение** и отдаёт его новым подписчикам.
	 Идея:`Subject` — поток событий.  
		`BehaviorSubject` — поток + память.

4. ngOnDestroy()
	`ngOnDestroy()` вызывается перед тем, как компонент удаляется.  
	Ты там делаешь “уборку”: отменяешь подписки, таймеры, очищаешь ресурсы.

5. switchMap
	**switchMap** — оператор из RxJS, который позволяет **переключать потоки**.
	Представь:  
	Ты делаешь запрос на поиск, и пользователь вводит новую букву.  
	Ты не хочешь, чтобы старый запрос продолжал работать — `switchMap` отменяет старый поток и запускает новый.


###### Вопросы
| №   | Вопрос                                                                                            | Ответ                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Что такое Angular и зачем он нужен?                                                               | Angular — фреймворк на TypeScript от Google для создания **single-page-приложений (SPA)**. Он вводит компонентно-ориентированную архитектуру, управление состоянием, маршрутизацию, формы и многое другое. ([interviewbit.com](https://www.interviewbit.com/angular-interview-questions/?utm_source=chatgpt.com "Top 40+ Angular Interview Questions and Answers (2025)"))                                                                          |
| 2   | Что такое @NgModule?                                                                              | `@NgModule` — декоратор, который группирует связанные компоненты, директивы, пайпы и сервисы в один логический модуль приложения. Он описывает, что в модуле **объявлено**, **импортировано**, **экспортировано** и из какого модуля стартует приложение (`bootstrap`). ([GeeksforGeeks](https://www.geeksforgeeks.org/angular-interview-questions-and-answers/?utm_source=chatgpt.com "Angular Interview Questions and Answers \| GeeksforGeeks")) |
| 3   | Объясните двухстороннее связывание данных (two-way data binding) в Angular.                       | Двухстороннее связывание данных (`[(ngModel)]`) позволяет синхронизировать данные компонента и шаблона: если свойство компонента меняется — шаблон обновляется, если пользователь вводит в шаблоне — свойство компонента меняется.                                                                                                                                                                                                                  |
| 4   | Что такое зависимость (Dependency) / внедрение зависимостей (Dependency Injection, DI) в Angular? | DI — паттерн, при котором нужные объекты (сервисы) **инжектятся** в класс (например, компонент или сервис) извне, а не создаются внутри. Это повышает модульность и тестируемость. Angular сам управляет инжекцией через провайдеры. ([GeeksforGeeks](https://www.geeksforgeeks.org/angular-interview-questions-and-answers/?utm_source=chatgpt.com "Angular Interview Questions and Answers \| GeeksforGeeks"))                                    |
| 5   | Перечислите жизненные хуки (lifecycle hooks) компонента Angular и зачем они нужны.                | Например: `ngOnInit()` (когда компонент инициализировался), `ngOnChanges()` (когда изменились входные @Input свойства), `ngOnDestroy()` (когда компонент уничтожается) и др. Они дают точки входа в жизненный цикл компонента.                                                                                                                                                                                                                      |
| 6   | Что такое Observable и как он отличается от Promise в Angular?                                    | Observable — это поток данных, который может эмитить несколько значений с течением времени, поддерживает отмену и цепочки операторов (RxJS). Promise — одиночное значение, без встроенной отмены. Angular много работает с Observable. ([DEV Community](https://dev.to/renukapatil/angular-interview-questions-4i4k?utm_source=chatgpt.com "Angular interview questions - DEV Community"))                                                          |
| 7   | Что такое AOT-компиляция (Ahead-of-Time) и её преимущества?                                       | AOT — компиляция шаблонов и компиляция TypeScript в JavaScript **до** запуска приложения в браузере. Преимущества: быстрее загрузка, меньше ошибок во время выполнения, оптимизация. ([interviewbit.com](https://www.interviewbit.com/angular-interview-questions/?utm_source=chatgpt.com "Top 40+ Angular Interview Questions and Answers (2025)"))                                                                                                |
| 8   | Чем отличаются директивы структурные и атрибутные?                                                | Структурные директивы (`*ngIf`, `*ngFor`) изменяют **структуру DOM** (добавляют/удаляют элементы). Атрибутные (`[ngClass]`, `[ngStyle]`) изменяют **поведение или стиль** существующего элемента. ([DEV Community](https://dev.to/renukapatil/angular-interview-questions-4i4k?utm_source=chatgpt.com "Angular interview questions - DEV Community"))                                                                                               |
| 9   | Что такое lazy-loading модулей в Angular и зачем он нужен?                                        | Lazy-loading позволяет загружать модули **по запросу** (только тогда, когда они нужны), что снижает время первоначальной загрузки приложения и улучшает производительность.                                                                                                                                                                                                                                                                         |
| 10  | Что такое change detection (обнаружение изменений) в Angular?                                     | Change detection — механизм, с помощью которого Angular проверяет изменения данных и обновляет представление (DOM). Он отслеживает, когда данные компонента изменились, и синхронизирует шаблон. ([interviewbit.com](https://www.interviewbit.com/angular-interview-questions/?utm_source=chatgpt.com "Top 40+ Angular Interview Questions and Answers (2025)"))                                                                                    |